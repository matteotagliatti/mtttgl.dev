---
import { CONFIG } from "../lib/consts";
import { fetchTopTracks } from "../lib/spotify";
import VinylCover from "./vinyl-cover.astro";

const { profileUrl, entries } = await fetchTopTracks(CONFIG.SPOTIFY.limit);
---

<section
  class="flex flex-col gap-4 rounded-lg bg-card p-4 sm:flex-row sm:items-center sm:justify-between"
>
  <div class="min-w-0">
    <a href={profileUrl} target="_blank" rel="noopener noreferrer">
      {CONFIG.SPOTIFY.title}
    </a>
    <p class="text-xs text-muted">{CONFIG.SPOTIFY.description}</p>
  </div>

  <div
    class="w-full overflow-x-auto sm:w-auto sm:overflow-visible [-ms-overflow-style:none] scrollbar-none [&::-webkit-scrollbar]:hidden"
  >
    <div class="flex w-max pl-1 sm:pl-3">
      {
        entries.map((entry, index) => (
          <a
            href={entry.link}
            target="_blank"
            rel="noopener noreferrer"
            class:list={[
              "group/vinyl relative block h-12 w-12 shrink-0 transition-transform duration-200 ease-out hover:-translate-y-1.5 hover:z-10",
              index > 0 && "-ml-2",
            ]}
            style={`z-index: ${entries.length - index}`}
            title={`${entry.title} — ${entry.artist}`}
          >
            <VinylCover albumArtUrl={entry.albumArtUrl} title={entry.title} />
          </a>
        ))
      }
    </div>
  </div>
</section>
