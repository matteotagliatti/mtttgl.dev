---
import { CONFIG } from "../lib/consts";
import { fetchGitHubCompany } from "../lib/github";

const company = await fetchGitHubCompany(CONFIG.PROJECTS.githubUsername);
---

{
  company && (
    <>
      {", currently working at "}
      <span class="font-medium">{company}</span>
    </>
  )
}
